{% extends 'base.html.twig' %}

{% block title %}LOL{% endblock %}

{% block body %}
    <h1>Événements LOL</h1>
    <ul>
        {% for event in events %}
            <li>
                <strong>{{ event.title }}</strong><br>
                <em>{{ event.date|date('d-m-Y H:i') }}</em><br>
                <p>{{ event.description }}</p>
                <p>Jeu: {{ event.jeu }}</p>
            </li>
        {% else %}
            <li>Aucun événement futur pour LOL.</li>
        {% endfor %}
    </ul>
{% endblock %}
