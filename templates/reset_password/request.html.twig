{% extends 'base.html.twig' %}

{% block body %}
    <h1>Réinitialiser le mot de passe</h1>

    {{ form_start(requestForm) }}
    {{ form_row(requestForm.email) }}
    <button class="button-container" type="submit">Envoyer</button>
    {{ form_end(requestForm) }}
{% endblock %}
