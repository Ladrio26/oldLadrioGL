{% extends 'base.html.twig' %}

{% block body %}
    <h1>Réinitialiser le mot de passe</h1>

    {{ form_start(resetForm) }}
    {{ form_row(resetForm.plainPassword) }}
    <button class="button-container" type="submit">Réinitialiser</button>
    {{ form_end(resetForm) }}
{% endblock %}
